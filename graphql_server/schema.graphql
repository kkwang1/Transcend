type Query {
  searchTerm(term:String, options:OptionInput): [Result]
  allResults: [Result]
}

input OptionInput {
  category: [[Option]]!
}

input Option {
  id: ID!
  text: String!
}

type Result {
  name: String!
  href: String!
  category: String!
}